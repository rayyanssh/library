#include <bits/stdc++.h>
using namespace std;

void first() {
    
}
void second() {

}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string command; cin >> command;
    if (command == "first") {
        first();
    } else {
        second();
    }
    return 0;
}
